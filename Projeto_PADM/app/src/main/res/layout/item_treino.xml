<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout de um item individual da lista de treinos (utilizado no RecyclerView).
    Cada item é representado por um CardView que contém informações resumidas sobre o treino,
    incluindo ícone, categoria, data e um ícone indicativo de detalhe.
-->
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp"
    android:foreground="?android:attr/selectableItemBackground"
    android:clickable="true">

    <!-- Estrutura principal horizontal do item -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:gravity="center_vertical">

        <!-- Ícone representando a categoria do treino -->
        <ImageView
            android:id="@+id/imgIcon"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_marginEnd="12dp"
            android:contentDescription="Icone" />

        <!-- Bloco de texto contendo categoria e data -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_weight="1">

            <!-- Nome da categoria do treino -->
            <TextView
                android:id="@+id/textCategoria"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textStyle="bold"
                android:textSize="16sp" />

            <!-- Data do treino -->
            <TextView
                android:id="@+id/textData"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#777777"
                android:textSize="14sp" />
        </LinearLayout>

        <!-- Ícone de seta indicando detalhes adicionais -->
        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/detail_arrow" />
    </LinearLayout>

</androidx.cardview.widget.CardView>
